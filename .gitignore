node_modules/
dist/
.DS_Store
*.local
.claude/skills/gstack/
.gstack/

# Staging area for the velocare removal (2026-08-21). The full legacy tree is
# recoverable from the `velocare-archive` branch and the `velocare-final-state`
# tag; this directory is safe to delete outright:  rm -rf _to_delete
_to_delete/

# Copied out of node_modules by scripts/vendor-mediapipe.mjs.
# Version-locked to package.json, so it is generated rather than committed.
public/vendor/

# Screenshots from tools/shoot.mjs and tools/offline-test.mjs.
shots/
